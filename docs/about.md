# Проблема масштаба 
# Big Data в чистом виде: Датасет включает в себя 80+ признаков для миллионов строк. Обычный Excel или простой скрипт на Python «падает» от нехватки памяти.
# 2. Техногенный «мусор» (Traffic Artifacts)
# NaN и Infinity: В полях вроде Flow Packets/s или Flow Bytes/s полно бесконечных значений, которые «вешают» математические модели.
# Битые сокеты: Встречаются записи, где порты источника или назначения указаны некорректно (например, 0), что невозможно в реальном TCP-соединении.

